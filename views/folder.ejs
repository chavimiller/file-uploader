<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= folder.name %> - Files</title>
  </head>
  <body>
    <header>
      <h1>Folder:<!--<%= folder.name %> --></h1>

      <nav>
        <a href="/home">← Back to folders</a>
      </nav>
    </header>

    <main>
      <!-- Upload file form for THIS folder -->
      <section>
        <h2>Upload a file</h2>
        <form
          action="/folder/<%= folder.id %>/file/new"
          method="POST"
          enctype="multipart/form-data"
        >
          <input type="file" name="uploaded_file" required />
          <input type="text" name="name" placeholder="File name" required />
          <button type="submit">Upload</button>
        </form>
      </section>

      <!-- Files in this folder -->
      <section>
        <h2>Files in this folder</h2>

        <% if (files && files.length > 0) { %>
        <ul>
          <% files.forEach(file => { %>
          <li>
            <%= file.name || file.originalName %>
            <div>
              <div>
                <a href="/folder/<%= folder.id %>/file/:fileId/download"
                  >Download</a
                >
              </div>
              <div>
                <a href="/folder/<%= folder.id %>/file/:fileId/delete"
                  >Delete</a
                >
              </div>
            </div>
          </li>
          <% }) %>
        </ul>
        <% } else { %>
        <p>No files in this folder yet. Upload one above!</p>
        <% } %>
      </section>
    </main>
  </body>
</html>
